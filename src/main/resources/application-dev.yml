server:
  port: 8080

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/ms_test?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

#mybatis:
#  mapper-locations: classpath:com/ms/service/mapper/*Mapper.xml
#  type-aliases-package: com.ms.service.bean

#showSql
logging:
  level:
    com:
      example:
        mapper : debug